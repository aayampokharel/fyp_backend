package main

import "github.com/aayampokharel/fyp/features/block"

// Middleware to log requests with colors

func main() {
	// router := chi.NewRouter()
	// router.Get("/", func(w http.ResponseWriter, r *http.Request) {})//todo: make these more shorter , use this func signature directly for handler if possible .
	block.CreateBlock()
}
